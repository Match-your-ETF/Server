version: '3.8'

services:
  fastapi-app:
    image: jisoo708/fastapi-app:latest
    container_name: fastapi-container
    restart: always
    env_file:
      - .env
    ports:
      - "80:8000"
    volumes:
      - /home/ubuntu/Server/.env:/Server/.env
